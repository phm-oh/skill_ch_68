# 📋 Test Checklist - ตามเกณฑ์การแข่งขัน 3.2.7

## ⚠️ หมายเหตุสำคัญ
**การทดสอบนี้เป็น Manual Testing** - กรรมการจะทดสอบด้วยมือในเครื่องของกรรมการเอง
**ไม่ต้องเขียน Script Automatic Test** - แค่ทำตาม checklist นี้ตอนกรรมการมาตรวจ

---

## 3.2.7.1 ทดสอบระบบ Login

### ✅ Test Case:
- [ ] **ทดสอบ Login**: 
  - Login ด้วย Username/Password **ถูกต้อง** → ต้อง Login สำเร็จ และ Redirect ไปหน้าที่ถูกต้อง
  - Login ด้วย Username/Password **ผิด** → แสดง error message

### Test Accounts:
- Admin: `admin@ccollege.ac.th` / `password123`
- Evaluator: `evaluator1@ccollege.ac.th` / `password123`
- Evaluatee: `teacher1@ccollege.ac.th` / `password123`

---

## 3.2.7.2 ทดสอบการ Insert ข้อมูล

### ✅ Test Case:
- [ ] **ทดสอบ Insert**: 
  - สร้างข้อมูลใหม่ (เช่น ผู้ใช้, รอบการประเมิน, หัวข้อการประเมิน) → ต้องสร้างสำเร็จ
  - สร้างข้อมูลซ้ำ (เช่น Email/Code ที่มีอยู่แล้ว) → แสดง error message

---

## 3.2.7.3 ทดสอบการ Update ข้อมูล

### ✅ Test Case:
- [ ] **ทดสอบ Update**: 
  - แก้ไขข้อมูลเดิม (เช่น ชื่อผู้ใช้, ชื่อรอบการประเมิน, คะแนน) → ต้องอัปเดตสำเร็จ
  - ตรวจสอบผลลัพธ์หลัง Update → ข้อมูลเปลี่ยนตามที่แก้ไข

---

## 3.2.7.4 ดำเนินการ Validate ข้อมูลก่อนส่ง

### ✅ Test Case:
- [ ] **ทดสอบ Validate**: 
  - ตรวจสอบช่องว่าง (required fields) → แสดง error ถ้าไม่กรอก
  - ตรวจสอบรูปแบบอีเมล → แสดง error ถ้ารูปแบบผิด
  - ตรวจสอบความยาวข้อมูล → แสดง error ถ้ายาว/สั้นเกินไป
  - ตรวจสอบรูปแบบอื่นๆ (เช่น คะแนน, วันที่) → แสดง error ถ้าไม่ถูกต้อง

---

## 3.2.7.5 ทดสอบฟังก์ชันเฉพาะจุด

### ✅ Test Case:
- [ ] **ทดสอบฟังก์ชันเฉพาะ**: 
  - **ฟังก์ชันคำนวณคะแนน** → คะแนนคำนวณถูกต้อง (เช่น คะแนนรวม, คะแนนตาม Topic)
  - **ฟังก์ชันการเรียงข้อมูล** → เรียงข้อมูลถูกต้อง (เช่น เรียงตามชื่อ, วันที่, คะแนน)

---

## 3.2.7.6 ทดสอบ Service API อย่างน้อย 3 รายการ

### ✅ Test Cases (ต้องทดสอบอย่างน้อย 3 วิธี):
- [ ] **POST API**: ทดสอบ POST `/api/auth/login` หรือ POST `/api/users` → Status 200/201, ได้ response ถูกต้อง
- [ ] **GET API**: ทดสอบ GET `/api/users` หรือ GET `/api/periods` → Status 200, ได้ข้อมูลถูกต้อง
- [ ] **PUT API**: ทดสอบ PUT `/api/users/:id` หรือ PUT `/api/periods/:id` → Status 200, อัปเดตสำเร็จ

### วิธีทดสอบ:
1. เปิด Browser DevTools → Network tab
2. ทำ action ในระบบ (เช่น Login, สร้างข้อมูล, แก้ไขข้อมูล)
3. ดู Network tab → ตรวจสอบ API request/response
4. ตรวจสอบ Status code, Response data

---

## 3.2.7.7 ใช้ Inspect/DevTools ตรวจสอบ Console และ Network

### ✅ Test Case:
- [ ] **ทดสอบ DevTools**: 
  - เปิด Browser DevTools (F12) → **Console tab** → ตรวจสอบว่าไม่มี Error (สีแดง)
  - เปิด Browser DevTools → **Network tab** → ตรวจสอบว่าไม่มี failed requests
  - ตรวจสอบ API calls → Status code เป็น 200/201/204 (ไม่ใช่ 400/500)

---

## 3.2.7.8 ทดสอบการแสดงผลแบบ Responsive

### ✅ Test Case:
- [ ] **ทดสอบ Responsive**: 
  - เปิด Browser DevTools → **Toggle device toolbar** (Ctrl+Shift+M)
  - ทดสอบแสดงผลบน **Mobile** (375px) → แสดงผลถูกต้อง, ปุ่มกดได้, scroll ได้
  - ทดสอบแสดงผลบน **Desktop** (> 1024px) → แสดงผลถูกต้อง
  - ตรวจสอบว่า UI ไม่แตก, ข้อความอ่านได้, ปุ่มกดได้

---

## 📝 หมายเหตุสำคัญ

### ⚠️ การทดสอบนี้เป็น **Manual Testing**
- **กรรมการจะทดสอบด้วยมือ** ในเครื่องของกรรมการเอง
- **ไม่ต้องเขียน Script Automatic Test** แยกต่างหาก
- แค่ทำตาม checklist นี้ตอนกรรมการมาตรวจ

### 🛠️ Tools ที่ใช้:
- **Browser DevTools** (F12) - สำหรับตรวจสอบ Console และ Network
- **Responsive Design Mode** (Ctrl+Shift+M) - สำหรับทดสอบ Responsive
- **Postman/Thunder Client** (ถ้าต้องการทดสอบ API โดยตรง)

### 📊 Test Data:
- ใช้ test accounts ที่มีใน `schema.sql`
- ใช้ seed data ที่มีใน `schema.sql`

### ✅ วิธีใช้ Checklist:
1. กรรมการจะเปิดระบบและทดสอบตาม checklist
2. ทำเครื่องหมาย ✅ ใน checkbox เมื่อทดสอบเสร็จ
3. บันทึกผลการทดสอบ (ผ่าน/ไม่ผ่าน)
4. บันทึก bugs หรือ issues ที่พบ (ถ้ามี)

---

**Last Updated**: 2025-01-XX
**Version**: 2.0 (Simplified - 1 case per requirement)

